using System;

class HeapOrdenarClase {
    static void HeapOrdenar(int[] arreglo) {
        // Implementación del algoritmo HeapOrdenar
    }

    static void Main() {
        int[] arreglo = {5, 2, 9, 1, 5, 6};
        HeapOrdenar(arreglo);
        foreach (int num in arreglo) Console.Write(num + " ");
    }
}
