using System;

class RadixOrdenarClase {
    static void RadixOrdenar(int[] arreglo) {
        // Implementación del algoritmo RadixOrdenar
    }

    static void Main() {
        int[] arreglo = {5, 2, 9, 1, 5, 6};
        RadixOrdenar(arreglo);
        foreach (int num in arreglo) Console.Write(num + " ");
    }
}
