using System;

class ComparacionOrdenarClase {
    static void ComparacionOrdenar(int[] arreglo) {
        // Implementación del algoritmo ComparacionOrdenar
    }

    static void Main() {
        int[] arreglo = {5, 2, 9, 1, 5, 6};
        ComparacionOrdenar(arreglo);
        foreach (int num in arreglo) Console.Write(num + " ");
    }
}
