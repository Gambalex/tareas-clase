using System;

class SeleccionOrdenarClase {
    static void SeleccionOrdenar(int[] arreglo) {
        // Implementación del algoritmo SeleccionOrdenar
    }

    static void Main() {
        int[] arreglo = {5, 2, 9, 1, 5, 6};
        SeleccionOrdenar(arreglo);
        foreach (int num in arreglo) Console.Write(num + " ");
    }
}
