using System;

class ConteoOrdenarClase {
    static void ConteoOrdenar(int[] arreglo) {
        // Implementación del algoritmo ConteoOrdenar
    }

    static void Main() {
        int[] arreglo = {5, 2, 9, 1, 5, 6};
        ConteoOrdenar(arreglo);
        foreach (int num in arreglo) Console.Write(num + " ");
    }
}
