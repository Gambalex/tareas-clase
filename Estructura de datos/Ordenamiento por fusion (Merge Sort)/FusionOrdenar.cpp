#include <iostream>
using namespace std;

void FusionOrdenar(int arreglo[], int n) {
    // Implementación del algoritmo FusionOrdenar
}

int main() {
    int arreglo[] = {5, 2, 9, 1, 5, 6};
    int n = sizeof(arreglo) / sizeof(arreglo[0]);
    FusionOrdenar(arreglo, n);
    for (int i = 0; i < n; i++) cout << arreglo[i] << " ";
}
