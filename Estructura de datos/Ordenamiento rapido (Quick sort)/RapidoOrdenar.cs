using System;

class RapidoOrdenarClase {
    static void RapidoOrdenar(int[] arreglo) {
        // Implementación del algoritmo RapidoOrdenar
    }

    static void Main() {
        int[] arreglo = {5, 2, 9, 1, 5, 6};
        RapidoOrdenar(arreglo);
        foreach (int num in arreglo) Console.Write(num + " ");
    }
}
