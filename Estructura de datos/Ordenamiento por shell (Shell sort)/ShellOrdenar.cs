using System;

class ShellOrdenarClase {
    static void ShellOrdenar(int[] arreglo) {
        // Implementación del algoritmo ShellOrdenar
    }

    static void Main() {
        int[] arreglo = {5, 2, 9, 1, 5, 6};
        ShellOrdenar(arreglo);
        foreach (int num in arreglo) Console.Write(num + " ");
    }
}
